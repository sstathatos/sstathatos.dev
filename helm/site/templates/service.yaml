apiVersion: v1
kind: Service
metadata:
  name: site
  labels:
    {{- include "site.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "site.labels" . | nindent 4 }}
  ports:
    - port: 80
      targetPort: {{ .Values.containerPort }}
