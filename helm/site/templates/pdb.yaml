apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: site
  labels:
    {{- include "site.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "site.labels" . | nindent 6 }}
